## Packages
framer-motion | Smooth entry animations and page transitions
lucide-react | Beautiful iconography for features and steps

## Notes
The application requires an active backend endpoint for POST /api/reels/download to process the URLs.
Currently, the frontend will handle loading and error states gracefully if the backend is missing or returns errors.
